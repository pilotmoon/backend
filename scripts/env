#!/bin/zsh
set -e

function inject() {
  local component=$1
  [[ -f .env-$component ]] || op inject -i .env-$component-template -o .env-$component
}

inject rolo
inject rolotest
inject twix
inject twixtest
